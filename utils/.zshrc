export NVM_DIR=/home/rishab/.nvm
[ -s /home/rishab/.nvm/nvm.sh ] && \. /home/rishab/.nvm/nvm.sh  # This loads nvm
[ -s /home/rishab/.nvm/bash_completion ] && \. /home/rishab/.nvm/bash_completion  # This loads nvm bash_completion
"export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion"
